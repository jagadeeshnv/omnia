- name: {{ group_name }}
  description: "{{ group_name }}"

  file:
    encoding: plain
    content: |
      ## template: jinja
      #cloud-config
      merge_how:
      - name: list
        settings: [append]
      - name: dict
{{  vars['cloud_init_' + item] | to_nice_yaml(indent=2) | indent(6, first=true) }}
